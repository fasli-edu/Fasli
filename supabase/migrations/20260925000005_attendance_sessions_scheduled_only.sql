-- حصص "الحضور المبكر": لما طالب بيحضر مقدماً حصة مجموعته القادمة (قرار من نافذة قرارات
-- الحضور)، بتتنشأ الحصة دي بتاريخ مستقبلي عشان صف حضوره يتربط بيها. الحصة دي لسه ماحصلتش
-- فعلياً — فلازم فحص الغياب الدوري (check-session-absences) ماينزّلش بيها غياب على باقي
-- المجموعة بمجرد ما يجي تاريخها (لأن حساب مهلة الغياب بيبدأ من created_at وهو قديم).
--
-- scheduled_only = true  → الحصة معمولة مسبقاً ولسه ماتفتحتش؛ فحص الغياب بيتخطاها
-- بمجرد ما المدرس يفعّلها فعلياً (تفعيل القارئ عليها، أو اختيارها في الحضور اليدوي)، بيتحوّل
-- لـfalse ويتصفّر created_at ليبدأ حساب المهلة من لحظة الفتح الحقيقية.
alter table attendance_sessions add column if not exists scheduled_only boolean not null default false;
